import { Typography } from '@mui/material';
import React from 'react';

const Top: React.FC = () => (
            <Typography variant="h4">
              トップページ
              </Typography>
);

export default Top;
