import { Meta, IconGallery, IconItem } from '@storybook/blocks';
import * as Icons from '@storybook/icons';

<Meta title="style/Iconography" />

# アイコン

<IconGallery>
  {Object.entries(Icons)
    .filter(([name]) => name !== 'iconList')
    .map(([name, Icon]) => (
      <IconItem name={name}><Icon/></IconItem>
    ))}
</IconGallery>
